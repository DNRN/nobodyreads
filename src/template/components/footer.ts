export function footerCss(): string {
  return `.site-footer {
  padding: 2rem 0;
  border-top: 1px solid var(--border);
}

.site-footer p {
  font-size: 0.75rem;
  font-family: var(--font-mono);
  color: var(--muted);
}`;
}
