export function heroCss(): string {
  return `.site-hero {
  padding-top: 2.5rem;
}

.hero-title {
  line-height: 1.1;
  margin: 0;
  font-weight: inherit;
}

.hero-tagline {
  color: var(--muted);
  font-size: 1rem;
  margin-top: 0.65rem;
  max-width: 36rem;
}`;
}
